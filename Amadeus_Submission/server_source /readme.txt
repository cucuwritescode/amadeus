basic pitch server source
facundo franchino, 2026

this folder contains my custom code for interfacing with spotify's basic pitch model.
includes audio pre-filtering, chord post-processing, and api endpoints to communicate
with the ios app.

note: as the server is not properly bundled for this submission, the compiled app will fallback to simulation mode instead of real neural network inference.
